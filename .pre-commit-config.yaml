repos:
  - repo: local
    hooks:
      - id: jupytext-sync
        name: Sync Jupytext Notebooks
        entry: uv run jupytext --sync --pre-commit-mode
        language: system
        files: '^experiments/.*\.py$'
        pass_filenames: true
